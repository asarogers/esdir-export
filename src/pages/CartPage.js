import Navbar from "../components/Navbar";
const Cart = ({ order, setOrder, robotPosition , setRobotPosition }) => {

  return (
    <>
              < Navbar/>
              cart page
    </>
  );
};

export default Cart;
                                        